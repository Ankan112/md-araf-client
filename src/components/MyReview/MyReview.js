import React from 'react';
import useTitle from '../../hooks/useTitle';

const MyReview = () => {
    useTitle('Review')
    return (
        <div>
            <h1>my review page here</h1>
        </div>
    );
};

export default MyReview;